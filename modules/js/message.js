/* Función JavaScript
   Si tenemos una función Javascript para que esta pueda ser reconocida por TypeScript
   y pueda usarse sin tener que reescribirla en TypeScript debe crearse un archivo de
   declaración con la extensión [.d.ts] */

function showMessage( message ) {
  alert( message );
}
